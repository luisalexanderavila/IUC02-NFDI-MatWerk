[project]
name = "creep-shacl-maker"
version = "0.1.0"
description = "a module to convert creep experiments from json file to shacl graphs"
authors = [
    {name = "Mariano Forti",email = "mariano.forti@icams.rub.de"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
